<UserControl x:Class="DatabaseController.View.ViewLogIn"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DatabaseController.View"
             xmlns:resx="clr-namespace:DatabaseController.Properties"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="200" Background="AliceBlue"
             
             x:Name="View">
    <Grid>
        <StackPanel Background="#FFDDCEB0">
            <Label Content="{Binding Source={x:Static resx:Resources.LoginLabel}}" Margin="16,30,20,0" FontFamily="Charlesworth" FontSize="14"/>
            <TextBox Text="{Binding Login, ElementName=View}" Margin="20,0,20,0" Height="20"/>

            <Label Content="{Binding Source={x:Static resx:Resources.PasswordLabel}}" Margin="16,10,20,0" FontFamily="Charlesworth" FontSize="14"/>
            <TextBox Text="{Binding Password, ElementName=View}" Margin="20,0,20,0" FontFamily="password" Height="20" />

        </StackPanel>
    </Grid>
</UserControl>
